# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'ShareCost' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RealmSwift'
  pod "PromiseKit", "~> 6.0"
  pod 'Alamofire', '~> 4.7'
  pod 'PromiseKit/Alamofire', '~> 6.0'
  pod 'Firebase/Analytics'
  pod 'Firebase/Core'
  pod 'Firebase/Storage'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'
  pod 'MessageKit'
  pod 'MessageInputBar'

  # Pods for ShareCost
  
  target 'ShareCostTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ShareCostUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
